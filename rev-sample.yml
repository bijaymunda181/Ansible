- name: Install Nginx
  hosts: app
  become: true
  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest
    - name: start Nginx
      ansible.builtin.systemd_service:
        name: nginx
        state: started

